class interface{
	field int i;
	field int x;
	field Kan kan;
	field Cat cat;
	constructor	interface new(){
		 	let i=0;
			while(i<512){
				do Screen.drawPixel(i,70);
				let i=i+1;
			}
			let x = 60;
			let kan = Kan.new();
			let cat = Cat.new();
			do kan.draw(30,100,1);
			do cat.draw(30+x,100,1);
			return this;
	}		
}
